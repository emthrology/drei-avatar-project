// VRMA 클립 카탈로그
//
// ⚠️ **부분 추출은 하지 않는다 (실측으로 반려).** VRMA 모션은 전신이 함께 움직인다는 전제로
// 저작돼 있어서, 부위를 떼어 쓰면 남은 부위와 균형이 깨져 오히려 더 어색해진다.
// (VRMA_02 실측: hips 앞숙임 +19~30° 와 head 젖힘 −16~−25° 가 짝. 전신을 다 가져오면 상쇄되지만
//  hips 만 가져오면 몸이 앞으로 숙고, head 만 가져오면 고개가 뒤로 젖혀진다. 부위 마스킹·상대
//  모드·가중치 축소를 모두 시도했고 전부 더 나빴다 — 상세 docs/vrma-adoption.md)
//
// → 대신 **전신을 통째로 쓰되 가장 정적인 클립을 고른다.** 7종 실측에서 VRMA_03(브이사인)이 최소
//   (hipsY 0.023m · 수평 0.067m · hips 81° · torso 67°). 이걸 개조한 게 wave.vrma 다.
//
// 표정·시선은 마스킹조차 불필요 — 공식 7종에 expression·lookAt 채널이 아예 없다(실측).
// 그래서 무드·립싱크·사케이드가 VRMA 재생 중에도 그대로 산다. 우리가 만들 .vrma 도 이 성질을
// 유지해야 한다(표정을 파일에 굽지 말 것 — 무드는 런타임 상태다).

export interface VrmaClipDef {
  id: string;
  label: string;
  url: string;
  /** 구간 트림(초). 공식 7종은 전신 쇼케이스라 쓸 구간이 일부다 */
  from?: number;
  to?: number;
  /** 진입/복귀 블렌드(ms) — 복귀 블렌드가 "동작 후 idle 로 돌아오기"를 담당 */
  fadeIn?: number;
  fadeOut?: number;
  /**
   * hips 이동 트랙 유지(기본 false = 제거). 7종은 전신 카메라를 전제로 저작돼 몸이 최대
   * 0.34m 이동/0.63m 상하하는데, 컴패니언은 로드 시 1회 산출한 고정 상반신 프레이밍이라
   * 그대로 재생하면 프레임을 벗어난다(실측). 회전은 그대로 둔다 — 전신 일관성이 깨지므로.
   */
  keepHipsPosition?: boolean;
  /**
   * 재생 동안 함께 걸 무드(표정). 재생 시작에 적용하고 끝나면 `moodAfter` 로 되돌린다.
   *
   * **본이 아니라 표정 레이어라서 합성이 성립한다** — 공식 7종에도 우리 wave.vrma 에도
   * expression 트랙이 아예 없어서(파일 상단 참조) mixer 가 표정을 안 건드린다. 그래서 무드의
   * held 표정·일회성 눈웃음이 클립 재생 중에도 그대로 산다. 반대로 본은 mixer 가 통째로
   * 덮어쓰므로, 무드의 **루프 톤**(호흡·머리 템포)은 재생 중엔 안 보이고 복귀 후에 반영된다.
   */
  mood?: string;
  /** 재생 종료 후 되돌릴 무드 (기본 neutral) */
  moodAfter?: string;
  /**
   * 손바닥 최소 간격(m) 강제 — **접촉 동작 전용**(박수). 재생 중 두 손바닥이 이 값보다
   * 가까워지면 양 상완을 바깥으로 돌려 되민다([palmContact.ts](./palmContact.ts)).
   *
   * 접촉은 **위치 제약**인데 VRMA 는 회전만 담아서, 같은 클립이 체형마다 다르게 닿는다.
   * 에셋에 상수 오프셋을 굽는 방식은 반려됐다 — 전 구간이 왜곡되고, 두 체형을 동시에
   * 만족시키는 값이 아예 없었다(실측: 남자1 최적 0° / 여자1 최적 6°).
   *
   * **미지정이면 no-op** — 손인사·인사 등 기존 클립은 출력 바이트 동일(개발 원칙①).
   */
  minPalmGap?: number;
}

// ❌ **구간 반복(`repeat`)은 반려됐다 (2026-08-11, 육안).** 박수가 짧아 보여서 트림을 한 주기의
// 같은 위상에서 끊고 2회 반복시켜 봤는데 **동작이 매우 어색했다.** 이음매 자세를 맞춰도 모캡
// 한 구간이 그대로 되풀이되면 사람 눈에 기계적으로 읽힌다(실제 박수는 매 번 조금씩 다르다).
// → 길이는 **트림 구간과 블렌드 시간**으로 조절한다. 짧으면 fade 를 줄여 full-weight 를 늘릴 것.

/**
 * hips 이동 트랙 제거 — 순수 함수(테스트 대상). 트랙 이름 형식은 `<노드명>.<property>`.
 * 여기가 틀리면 아바타가 재생 중 프레임 밖으로 나간다.
 */
export function filterTracks<T extends { name: string }>(
  tracks: readonly T[],
  dropPosition: boolean,
): T[] {
  if (!dropPosition) return [...tracks];
  return tracks.filter((t) => {
    const dot = t.name.lastIndexOf('.');
    return (dot < 0 ? '' : t.name.slice(dot + 1)) !== 'position';
  });
}

// ─── 카탈로그 ────────────────────────────────────────────────────────────────

/**
 * 손인사 — `wave.vrma`. VRMA_03(브이사인)을 [scripts/makeWaveVrma.mjs](../../../../scripts/makeWaveVrma.mjs)
 * 로 개조한 우리 저작물이다(손가락→편 손 이식, 손목→좌우 흔들기 주입). 재생성 가능하지만
 * 산출물을 커밋한다(썸네일과 같은 취급 — 빌드 서버에서 재생성하지 않는다).
 *
 * 트림 구간은 팔이 올라와 자세가 안정된 뒤(2.9s~). 팔을 드는 전환은 클립이 아니라 우리
 * fadeIn 블렌드가 만든다 — idle 팔 자세에서 출발하므로 그쪽이 더 매끄럽다.
 */
export const VRMA_WAVE: VrmaClipDef = {
  id: 'wave',
  label: '손인사(VRMA)',
  url: '/animations/wave.vrma',
  from: 2.9,
  to: 5.9,
  fadeIn: 400,
  fadeOut: 500,
};

/**
 * 인사 — 손인사(본) + happy(표정) 합성. **컴패니언 진입 시 1회** 재생된다(useVrmaLayer greetOnReady).
 *
 * 새 `.vrma` 파일이 아니다 — 같은 클립에 무드를 얹은 **구성**이다. 레이어가 분리돼 있어서
 * (본=VRMA / 표정=무드 시스템) 파일을 새로 저작하지 않고 조합만으로 만들어진다.
 * 표정을 `.vrma` 에 굽지 않는다는 원칙(파일 상단)의 실익이 여기서 나온다 — 구우면 무드별로
 * 파일이 하나씩 필요해진다.
 *
 * 무표정으로 손만 흔드는 것보다 인사답고, `happy` 진입의 일회성 눈웃음(HAPPY_EYE)이 클립
 * 길이(3s)와 맞물려 "웃으며 손 흔들고 → 평상시 표정으로" 가 한 번에 끝난다.
 */
export const VRMA_GREET: VrmaClipDef = {
  ...VRMA_WAVE,
  id: 'greet',
  label: '인사(손+미소)',
  mood: 'happy',
};

/**
 * 박수 — `clap.vrma`. **외부 조달본을 우리가 보정한 것**(wave.vrma 와 달리 원저작이 아니다).
 * 원본 `clap-src.vrma` 출처: https://github.com/tk256ailab/vrm-viewer `VRMA/Clapping.vrma`
 * (코드는 MIT, **에셋은 출처·권리 미표기**). 개인 연구용이라는 판단으로 반입했다 — 상업 전환 시
 * 재확인할 것. 보정은 [scripts/makeClapVrma.mjs](../../../../scripts/makeClapVrma.mjs)(손가락 펴기,
 * 결정적 재생성). **원본과 산출물을 둘 다 커밋한다**(VRMA_03+wave.vrma 와 같은 취급).
 *
 * 공식 7종에 박수가 없어서 외부에서 조달했다. 7종 중 양손이 만나는 건 VRMA_07 뿐인데
 * **손바닥이 서로를 안 향한다**(접촉축 정렬 0.14~0.43 — 이 파일은 0.87~1.00). 즉 "손이 만난다"
 * 이지 "손바닥끼리 친다"가 아니라서 개조 베이스로도 못 썼다.
 *
 * 실측(소스 리그 FK): hips yaw −1.5~+3.0°(정면 — 03 은 −22.9°, 07 은 +88°) · z +0.18(가슴 앞)
 * · **표정 트랙 0**(무드 합성 성질 유지) · 51본(손가락 30) 30fps.
 *
 * ⚠️ **접촉은 런타임이 맞춘다 (`minPalmGap`)** — 에셋에는 상수 오프셋을 굽지 않는다.
 * 회전만 리타게팅되므로 손이 닿는 위치는 체형이 정하는데(최접촉 손바닥 간격 남자1 0.0227 /
 * 여자1 0.0106 = 겹침), 상완을 N° 벌려 굽는 방식은 **전 구간을 왜곡하면서도 두 체형을 동시에
 * 만족시키는 값이 없었다**(남자1 최적 0° / 여자1 최적 6°, 0·3·5·6·7·10 전부 측정).
 * 그래서 에셋은 **원본 동작 그대로**(보정 스크립트는 손가락 펴기만) 두고, 파고드는 프레임에서만
 * 런타임이 되민다. **양 캐릭터 프로브(`npm run probe -- --clap`) 필수.**
 *
 * ⚠️ **트림은 뒤쪽 국면만 쓴다.** 원본은 두 국면이다 — 0.5~1.7s 는 손을 얼굴 높이로 **올려서**
 * 치고(y +0.33~0.40), 1.77~2.80s 는 가슴 앞으로 **내려서** 친다(y +0.21~0.25). 올린 박수는
 * 300×400 상반신 프레임에서 손이 얼굴에 붙어 과하다(육안). 2.80s 이후는 손을 내리는 구간이라
 * 잘랐다(2.73 부터 y 0.20→0.10). 시작을 1.55 로 앞당긴 건 **fadeIn 동안 팔이 올라올 활주로**를
 * 주기 위해서다 — 그 구간(y 0.335, 손을 내리는 중)의 접촉은 가중치가 낮아 거의 안 보인다.
 *
 * ⚠️ **fade 를 짧게 줄이지 말 것 — 컴패니언과 에디터의 출발 자세가 다르다.** 박수 횟수를 늘리려
 * 200ms 까지 줄였다가 **컴패니언에서만 팔이 튀어오르는** 문제가 났다(육안). 에디터는
 * `animPreview` 기본 OFF 라 정적 A-pose(팔이 이미 벌어짐)에서 출발해 이동거리가 짧지만,
 * 컴패니언은 idle armPose(차렷)라 팔을 옆구리에서 가슴까지 들어올려야 한다. wave 가 fadeIn 400 을
 * 쓰는 이유가 이것 — **idle 에서 팔을 드는 검증된 값**이다. 복귀도 같아서 fadeOut 도 길어야 한다.
 * → 길이는 fade 가 아니라 **트림 구간**으로 확보한다(그래서 1.55 부터).
 *
 * 팔을 드는 전환은 wave 와 같이 fadeIn 블렌드가 만든다(클립이 아니라).
 * (30fps 클립이지만 subclip 의 fps 인자는 `time*fps ≥ from*fps` 로 상쇄돼 FPS=60 상수로 정확하다)
 */
export const VRMA_CLAP: VrmaClipDef = {
  id: 'clap',
  label: '박수(VRMA)',
  url: '/animations/clap.vrma',
  from: 1.55,
  to: 2.8,
  fadeIn: 400,
  fadeOut: 400,
  // 손바닥이 이보다 가까워지지 않게 런타임 보정. 값은 육안 기준점에서 왔다 —
  // 0.011=겹쳐 보임 / 0.023=제대로 닿음(사용자 판정). 그 사이 여유를 둔 0.022.
  minPalmGap: 0.022,
};

export const VRMA_CLIPS: Record<string, VrmaClipDef> = {
  [VRMA_WAVE.id]: VRMA_WAVE,
  [VRMA_GREET.id]: VRMA_GREET,
  [VRMA_CLAP.id]: VRMA_CLAP,
};
